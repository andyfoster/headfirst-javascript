// We use a capital letter for the function name

function Dog(name, breed, weight) {
  this.name = name;
  this.breed = breed;
  this.weight = weight;

// this constructor returns "this" but we don't have to explicitly say that.
}
